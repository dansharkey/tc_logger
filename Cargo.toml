[package]
name = "thermocouple_logger"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.27", features = ["default"] }
egui = "0.27"
egui_plot = "0.27"
serialport = "4"
chrono = { version = "0.4", features = ["std"] }

[profile.release]
opt-level = 3
strip = true
